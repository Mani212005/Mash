# Force Python provider (frontend/package.json causes Node.js false detection)
providers = ["python"]

[variables]
NIXPACKS_PYTHON_VERSION = "3.11"

[start]
cmd = "python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080}"
