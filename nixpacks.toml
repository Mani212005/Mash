# Tell Railway/Nixpacks this is a Python project
# (needed because frontend/package.json makes it detect Node.js)

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080}"
